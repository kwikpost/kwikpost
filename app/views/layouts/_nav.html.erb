<nav class="navbar navbar-default" id="height_nav">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= image_tag("logo.png")  %></a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        
        <%= form_tag products_path, :method => :get, class: 'navbar-form', role:'search' do %>
          <a href=""><i class="fa fa-map-marker fa-lg" style="color: black"></i></a>
          <div class="form-group">
            <%= text_field_tag :search_location, params[:search_location], :id => 'geo-input', class: 'form-control', :style => 'border-radius: 10px' %>
            <%= submit_tag "Search", :name => nil, class: 'btn btn-default btn-sm form-control', :style => 'border-radius: 10px' %>
          </div>
        <% end %>
        
      </ul>
      <ul class="nav navbar-nav navbar-right">

        <li><a href="/mains/show" role="button" data-toggle="modal" id="font_color">About</a></li>
        <% if user_signed_in? %>
        <li><a href="/mains/<%= current_user.id %>/posts" role="button" data-toggle="modal" id="font_color">Posts</a></li>
          <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <img class="profile-img" src="<%= current_user.image %>"> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", user_path(current_user) %></li>
                <li><%= link_to "Inbox", mailbox_inbox_path %></li>
                <li><%= link_to "Settings", edit_user_registration_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
                </li>
              </ul>
          </li>
          <% else %>
            <li><a href="#loginModal" role="button" data-toggle="modal" id="font_color">Login</a></li>
            <li><a href="#signupModal" role="button" data-toggle="modal" id="font_color">Sign up</a></li>
        <% end %>
      </ul>
    </div>
</nav>


<script type="text/javascript">
  $(document).ready(function() {
    $('.dropdown-toggle').dropdown();
    $("#geo-input").geocomplete();
  });
</script>