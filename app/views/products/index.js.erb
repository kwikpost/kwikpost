alert('append');
var delimiter =
        $('<div class="page-delimiter" data-page="<%= params[:page] %>"></div>');
$('#my-products').append(delimiter);
$('#my-products').append('<%= j render @products %>');
$('.pagination').replaceWith('<%= j will_paginate @products %>');
preparePagination(delimiter);
<% unless @products.next_page %>
$('#infinite-scrolling .pagination').remove();
<% end %>